 int[] arr = { 3, 8, 7, 4, 9 };
 for (int i = 0; i < arr.Length - 1; i++)
 {
     for (int k = 0; k < arr.Length - 1; k++)
     {
         if (arr[k] > arr[k + 1])
         {
             int swap = arr[k];
             arr[k] = arr[k + 1];
             arr[k + 1] = swap;

         }
     }
 }
